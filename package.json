{
  "name": "agent-cli",
  "version": "0.1.0",
  "description": "从零手搓的仿 Claude Code 命令行 Agent CLI（学习 ReAct / Tool Calling / MCP / 多模型适配）",
  "type": "module",
  "bin": {
    "agent-cli": "dist/cli/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx src/cli/main.ts",
    "build": "tsup",
    "start": "node dist/cli/main.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
